{
  "name": "agent-runtime",
  "version": "1.0.0",
  "scripts": {
    "dev": "nodemon src/server.ts",
    "build": "tsc && npm run docker:build",
    "start": "node dist/server.js",
    "docker:build": "docker build -t zk-agent-runtime .",
    "akash:deploy": "akash provider send-manifest akash-manifest.yml"
  },
  "dependencies": {
    "express": "^4.18.0",
    "cors": "^2.8.5",
    "helmet": "^7.0.0",
    "dotenv": "^16.0.0",
    "@zk-agentmesh/zk-circuits": "*",
    "@zk-agentmesh/payment-layer": "*",
    "node-sgx": "^1.0.0",
    "circomlib": "^2.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/cors": "^2.8.0",
    "nodemon": "^3.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}